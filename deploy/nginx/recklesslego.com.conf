# nginx reverse proxy for recklesslego.com -> Next.js app on 127.0.0.1:3000.
#
# Install on the server:
#   sudo cp deploy/nginx/recklesslego.com.conf /etc/nginx/sites-available/recklesslego.com
#   sudo ln -s /etc/nginx/sites-available/recklesslego.com /etc/nginx/sites-enabled/
#   sudo nginx -t && sudo systemctl reload nginx
#
# This file is HTTP-only. Running `sudo certbot --nginx -d recklesslego.com -d www.recklesslego.com`
# rewrites it in place to add the TLS (443) server block and an HTTP->HTTPS redirect.

server {
    listen 80;
    listen [::]:80;
    server_name recklesslego.com www.recklesslego.com;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;

        # Preserve the original request info for the app.
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Allow WebSocket / connection upgrades.
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_cache_bypass $http_upgrade;
    }
}
